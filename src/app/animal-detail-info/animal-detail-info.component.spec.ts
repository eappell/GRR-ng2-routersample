/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AnimalDetailInfoComponent } from './animal-detail-info.component';

describe('Component: AnimalDetailInfo', () => {
  it('should create an instance', () => {
    let component = new AnimalDetailInfoComponent();
    expect(component).toBeTruthy();
  });
});
